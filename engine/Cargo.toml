[package]
name = "engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
lto = true
strip = true

[dependencies]
serde = { version = "1.0.213", features = ["derive"] }
tsify = "0.4.5"
wasm-bindgen = "=0.2.95"
